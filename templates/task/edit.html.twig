{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Task'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Edit Task'|trans }}</h1>

    {{ include('task/_form.html.twig', {'button_label': 'OK'}) }}

    <a href="{{ path('app_task_index') }}">{{ 'back to list'|trans }}</a>

{% endblock %}
